# مذكّرة تسليم — 2026-07-27

**الفرع:** `feature/overnight-reports-20260726` — **مدفوع بالكامل لـorigin ومنشور حياً**
على `C:\ZynoraPortal` (5080/5443) ويُخدَم عبر Cloudflare Tunnel على
`zynorahr.com` · `www.zynorahr.com` · `portal.zynorahr.com` (كلها 200).
الدخول: بيانات المسؤول تُسلَّم خارج المستودع (أُزيلت من التوثيق —
راجع `docs/SECURITY-CREDENTIAL-ROTATION.md`).

## أُنجز هذه الجلسة (7 كوميتات، كلها مدفوعة + منشورة)
| كوميت | الوصف | تحقّق |
|---|---|---|
| `0706762` | جرس إشعارات back-office (`SystemNotifications`، أدمن/HR) | E2E حيّ ✅ |
| `8b75a23` | توصيل «تعبئة البصمة المفقودة» بمحرك الاشتقاق (كان صمّاءً) | 10 اختبارات، **E2E حيّ مؤجَّل** (راية off) |
| `ad233b2` | ربط مؤشّري الإجازات/العهد بملف 360 | بناء أخضر (عرض للقراءة) |
| `a1c4342` | حذف خدمة SystemUser الميتة + تنقّل ميت | بناء أخضر + 279 اختبار |
| `6a5e091` | فحص: ربط الإجازات بالمسير + محرك الصيغ **مبنيّان أصلاً** | تقرير |
| `313f945` | جرس بوابة الموظف الموبايل (`UserNotification`، لكل موظف) | **E2E موبايل = اختبار محمد على هاتفه** |
| (docs) | هذه المذكّرة | — |

**الاختبارات: 279 أخضر. كل البناء أخضر.**

## معلّقات للمتابعة
1. **تحقّق E2E حيّ لـ«تعبئة البصمة المفقودة»** (`8b75a23`) قبل تفعيلها على مناوبة حقيقية —
   تمسّ اشتقاق الحضور ⟹ المسير. محروسة براية **off** افتراضياً (آمنة حتى تُفعَّل من `/ShiftTypes`).
2. **اختبار جرس الموبايل** (`313f945`) على هاتف حقيقي عبر `zynorahr.com` (دخول موظف).

## الفجوة الحقيقية المتبقّية (ميزة مستقلة كبيرة، تحتاج جلسة نظيفة)
الحقول الصمّاء **البنيوية** بـ`/ShiftTypes`: `TimeLimit`/`MidShift`/`StripSemantics`/تبويب
«التعارض مع الحركة». كلها تشترك بعائق واحد: تحتاج **تيار البصمات الخام** لا طرفَي اليوم
المطويّين في `BuildMonthAsync` (byDay). راجع ذاكرة `shift-type-dead-settings` (الطبقة (2)
من دالة الرصد النقية `MovementConflictPolicy` منجزة سابقاً، المتبقّي الطبقات 1+3+4).

## قواعد تشغيلية (مهمة)
- **النشر الحيّ يتطلّب موافقة صريحة لكل مرّة** — لا تعتبر «انشر» تفويضاً متكرراً
  (راجع ذاكرة `deploy-approval-per-action`). إجراء النشر: `deployment-static-assets-manifest`.
- بعد تعديل الكود: `graphify update .`
- الفجوات مقابل كيان = **صفر** (بنك/تقارير/إجازات-مسير/صيغ كلها منجزة).
