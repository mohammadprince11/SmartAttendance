# HANDOFF — 2026-08-09

Snapshot of repository state at the close of the People acceptance + Payroll salary-base sessions.

## Branch & remote

| Item | Value |
|---|---|
| Branch | `claude/smartattendance-local-rebuild-wftwb3` |
| HEAD | `28061f2` (local == `origin`, 0 unpushed) |
| `origin/main` | `73169433` (unchanged) |
| Ahead / behind `origin/main` | 40 ahead / 2 behind (the 2 behind are prior merges of this same branch, PR #19/#20) |
| Merge base | `d4085b3` |
| Tree | clean (only untracked `graphify-out/` cache) |

**PR: not yet opened.** `gh` is unauthenticated on this machine and the OAuth flow can't run non-interactively. Branch is pushed; open the PR manually:
`https://github.com/mohammadprince11/SmartAttendance/compare/main...claude/smartattendance-local-rebuild-wftwb3?expand=1`
Prepared title/body: session scratchpad `PR-BODY-payroll.md`.

## Build & tests

- Release build: **0 errors** (23 pre-existing nullability warnings, unrelated).
- Tests: **1496 passed, 0 failed, 0 skipped**.
- `git diff --check`: clean.

## Work item 1 — People module (acceptance)

- **Status: READY FOR MERGE PREPARATION.** Report: `docs/PEOPLE-FINAL-ACCEPTANCE-REPORT-2026-08-09.md` (commit `42b1549`).
- Verified: authorization/multi-company isolation (scope ∩ intersection, SQL-before-limit), canonical `ContractRegisterStore` (soft-delete, idempotent, single-current invariant), lifecycle (EndService/Rehire transactional+idempotent, AdministrativeDelete gated by operational-history guard), `ProtectedFileService` (no `UseStaticFiles`), compensation gating, dashboard metrics.
- No People-module release blocker. Merge to `origin/main` is conflict-free (`git merge-tree`).

## Work item 2 — Payroll salary-base engine correction

- **Status: READY FOR ACCEPTANCE TESTING.** Reports: `docs/PAYROLL-ENGINE-CORRECTION-REPORT-2026-08-09.md`, `docs/PAYROLL-ATTENDANCE-EFFECTS-MATRIX-2026-08-09.md`.
- 14 commits above `42b1549`, ending `28061f2`.

### Delivered
- **Six configurable salary bases**: Attendance (per-component via `SalaryItem.Prorated`), Tax & GOSI (composed *or* employee-defined), Overtime & UnpaidLeave (`PayrollEarningBase` ÷ `PayrollDivisorPolicy`), Penalty (pre-existing `BasePoolJson`).
- **Fully dynamic UI**: Salary Items (Prorated / OvertimeEligible / UnpaidLeaveEligible / GosiEligible flags), Payroll Settings (base modes + salary-days basis + daily hours), Financial Info (Tax/Gosi base modes + current tax salary).
- **Payslip trace** (AttendanceBase/factor, Tax/GOSI base + source) persisted per line, shown in RunDetail.
- **Double-deduction guard**, **per-allowance tax/GOSI** (`TaxableAllowances`/`GosiAllowances`), **config validation** (rates/brackets/salaries/hours), **rounding policy** pinned.
- Seven golden scenarios pass with exact decimals. Every default reproduces the previous numbers → the original 1451 regression tests and historical/locked runs are untouched.

### New pure units
`AttendanceSalaryBase`, `EmployeeDefinedSalaryBase`, `PayrollEarningBase`, `PayrollRateBasis`, `PayrollDivisorPolicy`, `PayrollConfigValidation`.

### Migrations (additive, idempotent, non-destructive)
`20260809-01` employee tax/gosi base modes · `20260809-02` salary-item OT/unpaid eligibility · `20260809-03` payroll-line trace · `20260809-04` salary-item gosi-eligible.

## Commit trail (above `42b1549`)

```
28061f2 docs(payroll): update report — rounding + attendance-effects complete (1496 green)
b62a50d test+docs(payroll): rounding policy edge cases + attendance-effects matrix
a9d8dfe docs(payroll): record config validation guardrails
ca9c57a feat(payroll): validate dynamic config — reject bad rates, brackets, salaries
a8e310b docs(payroll): record per-allowance tax/gosi + payslip trace complete
032ed75 feat(payroll): per-allowance tax/gosi eligibility + persisted payslip trace
81721e9 docs(payroll): finalize correction report — ready for acceptance testing
a5790db test(payroll): guard against double-deducting a single attendance event
031b02d feat(payroll): expose salary-base policies in the UI (dynamic config)
56e645d docs(payroll): record completed overtime/unpaid-leave bases and divisor policy
f3a0ea7 fix(payroll): unify unpaid-leave and overtime bases behind a divisor policy
dc769f2 docs(payroll): salary-base architecture correction report
0291391 fix(payroll): honor employee-defined gosi and tax salary bases
2940d4a fix(payroll): prorate attendance-eligible salary components
42b1549 docs(people): complete final people acceptance audit
```

## What remains (owner: Mohammad)

1. **Open the PR** (compare link above) — `gh` needs auth, or do it in the GitHub UI.
2. **Live two-company acceptance run** on a controlled **non-production** DB (Phase 2/16/29). Not done here because the dev server shares the production DB; running migrations/calcs there needs deploy authorization.
3. **Merge & deploy** — human decision. Migrations `20260809-01..04` apply on the next authorized deploy.

## Guardrails honored this session

No merge to main · no deploy · no force-push · no production data touched · no historical/locked payroll recalculated · no `TEST-PAY-001` inserted into production · the dev server was **not** launched (shared production DB).
