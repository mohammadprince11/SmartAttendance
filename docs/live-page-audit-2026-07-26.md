# جولة فحص حي صفحة-صفحة — 2026-07-26

الموقع: `http://localhost:5080` (النسخة الشغّالة) · حساب: admin
الطريقة: فتح كل صفحة حية ورصد (موجود / نقص / اقتراح تحسين). مرتّب بالأولوية داخل كل موديول.

الرموز: 🔴 نقص مؤثّر · 🟡 تحسين · 🟢 يشتغل تمام · 🐞 خلل ظاهر

---

## الموديول 1: بوابة الموظف + الإعدادات

### `/EmployeePortal` — بوابة الموظف الذكية

**موجود:** تبويبات (الرئيسية · ملفي · الحضور · الراتب · طلباتي · الاستبيانات · الشكاوي · التقييم والانضباط) + بطاقات ملخّص (دوام اليوم، إعلانات، استبيانات، طلبات، شكاوي) + حائط إعلانات موجّه + زر تثبيت PWA + تفعيل إشعارات.

**ملاحظات:**
- 🐞 **تعارض هوية بصرية:** البوابة معنونة "NEXORA Employee Experience · إصدار v7" بينما بقية النظام "ZYNORA HR". لازم توحيد الاسم (يفترض يقرأ من محرك الهوية/Branding).
- 🟡 شارة "وضع تجريبي — لم يتم العثور على مستخدم موظف مرتبط، عُرض أول موظف": منطقي للاختبار بحساب admin، بس لازم نتأكد إن ربط حساب↔موظف يشتغل صح بحساب موظف حقيقي.

**فحص التبويبات (8 تبويبات، كلها URL-driven `?tab=`):**
- 🟢 **ملفي الشخصي** (`tab=profile`): يقرأ من DB (الاسم، الرقم، المنصب، القسم، التعيين، الهاتف، البريد) — شغّال (DB Linked).
- 🟢 **الحضور** (`tab=attendance`): بصمة أونلاين (حضور/انصراف GPS) + ملخص + سجل يومي 26 يوم. شغّال.
- 🟢 **الراتب** (`tab=compensation`): يعرض "غير مدخل" للموظف التجريبي — سلوك صحيح (يظهر بعد اعتماد الكشف).
- 🟢 **طلباتي** (`tab=requests`): 5 أنواع طلبات (إجازة/نسيان بصمة/خروج شخصي/خروج عمل/أوفرتايم) + متابعة. DB Sync.
  - 🟡 **الطلب المالي غير مُدرَج بين أنواع الطلبات** رغم وجود صفحة `/EmployeePortal/FinancialRequest` منفصلة — تجربة مبعثرة؛ يفضّل دمجه ضمن منتقي أنواع الطلبات.
- 🟢 **الاستبيانات** (`tab=pulse`): استبيانات + انتخابات من Pulse Studio.
  - 🔴 **`Index.cshtml:655` مؤشر النبض `78%` قيمة ثابتة مكتوبة يدوياً** — مو محسوبة من بيانات حقيقية (نفس الرقم لكل موظف).
- 🟢 **الشكاوي والمقترحات** (`tab=feedback`): نموذج (نوع/أولوية/عنوان/تفاصيل) + متابعة ردود.
- 🔴 **التقييم والانضباط** (`tab=performance`): **تبويب placeholder غير مربوط**:
  - `Index.cshtml:716-717` "آخر تقييم — لم يُربط جدول التقييم بعد" + **نقاط القوة "الالتزام، التعاون، سرعة التعلم" قيمة ثابتة لكل الموظفين**.
  - `Index.cshtml:725` الانضباط "سيُعرض بعد ربط الجدول الرسمي" — **رغم وجود موديول `Violations` + `DisciplinaryRules` فعلي بالنظام غير مربوط بالبوابة**. أقرب مكسب: اربط تبويب الانضباط بجدول المخالفات الموجود.
  - لا يوجد موديول "تقييم أداء" حقيقي بعد (Performance Reviews) — فجوة مقابل ZenHR/كيان.

### `/HrSettings` — مركز الإعدادات (Hub)

**موجود:** 11 صفحة فرعية (فترة التجربة · الإنذار · الخدمة الذاتية · أسباب الإيقاف · مركز الإشعارات · القوائم المرجعية · مخطط رمز الموظف · استوديو الحقول · قوالب الموافقات · مركز أنواع الطلبات · حقول الكيانات). تغطية إعدادات واسعة جداً تنافس ZenHR/كيان.

### `/HrSettings/SelfServiceSettings` — إعدادات الخدمة الذاتية
- 🟢 غنية: منشورات الحائط، مجهولية الشكاوي، نطاق حالات الموظف، صلاحيات المدير حسب المستوى، إلزام المرفق/ملف الالتزام البنكي، طلب النقل، لقطات شاشة التطبيق، دليل الموظفين.
- 🐞 **تسريب مصطلح المنافس:** "جدار **كيان** الاجتماعي" + "بطاقات المواهب" — نص من كيان تسرّب للواجهة؛ لازم اسم عام أو اسم منتجنا.
- 🔴 **يتطلّب تدقيق استهلاك:** نفس فخّ [[shift-type-dead-settings]] — لازم نتأكد إن كل تبديل هنا **مقروء فعلاً** (مثلاً "منع الشكوى المجهولة" هل يُنفَّذ بتبويب الشكاوي بالبوابة؟ "نطاق حالات الموظف" هل مستهلَك؟).

### `/HrSettings/NotificationCenter` — مركز الإشعارات
- 🟢 شامل: ~20 مُطلِق (عيد ميلاد، ذكرى عمل، ترحيب، إعادة تعيين، وداع، عقود/تجديد/تمديد، تقاعد، فترة تجربة، مخالفات، إجراءات تأديبية، مقابلات نهاية خدمة، شكاوي، انتخابات...). لكل مُطلِق: المستلمون + عدد الأيام + العناصر.
- 🔴 **يتطلّب تدقيق تفعيل:** هل خدمة الإشعارات الخلفية ([[external-notification-channel]]) تقرأ هذي الضوابط وتُطلق فعلاً لكل نوع؟ أو الواجهة تحفظ فقط؟ (نفس نمط الإعدادات الصمّاء — أولوية تحقّق).

### `/HrSettings/RequestTypes` — مركز أنواع الطلبات
- 🟢 **داينمك حقيقي ومربوط بالبوابة:** تبويبات (إجازات/عرضية/مغادرات/أوفرتايم) + أنواع بضوابط (مدفوع/غير مدفوع، رصيد، وقت، يُستقطع). أضف نوع → يظهر بتطبيق الموظف. أفضل جزء بالموديول.

### `/Branding` — الهوية والثيم
- 🟢 محرّك White-Label كامل: اسم معروض + ألوان (رئيسي/ثانوي/تمييز) + سلّم درجات + شعار/favicon/خلفية دخول + معاينة حية + فحص تباين. الاسم الافتراضي **ZYNORA**.
- 🐞 **بقايا الاسم القديم NEXORA:** المنتج أُعيدت تسميته من NEXORA → ZYNORA، وبقيت آثار لا تمرّ عبر المحرّك:
  - `wwwroot/brand/nexora-logo.svg` · `nexora-symbol.svg` · `employee-photo-placeholder.svg` (تكتب "NEXORA HR").
  - **بوابة الموظف تعرض شعار/اسم NEXORA** بدل هوية ZYNORA/هوية الشركة من المحرّك ⟹ سبب تعارض الهوية أعلاه.
  - ثوابت/تعليقات كود (`PeopleAccessContext`، تعليقات CSS `NEXORA_FIX*`) — تجميلية، أقل أولوية.
  - **الإصلاح المؤثّر:** توحيد ترويسة البوابة + أصول الشعار لتقرأ من محرّك الهوية بدل NEXORA الثابت.

---

## ملخّص أولويات الموديول (بوابة الموظف + الإعدادات)

| # | البند | نوع | الأثر |
|---|-------|-----|-------|
| 1 | ~~تبويب "التقييم والانضباط" placeholder + قيم ثابتة~~ **✅ مُصلَح 2026-07-26** | 🔴→✅ | صار بيانات حقيقية |
| 2 | تدقيق تفعيل مركز الإشعارات (هل يُطلق فعلاً؟) | 🔴 | إشعارات لا تصل |
| 3 | تدقيق استهلاك إعدادات الخدمة الذاتية (صمّاء؟) | 🔴 | إعدادات بلا أثر |
| 4 | توحيد الهوية: إزالة بقايا NEXORA وربط ترويسة البوابة بمحرّك ZYNORA | 🐞 | تعارض علامة أمام العميل |
| 5 | تسريب مصطلح "كيان" بواجهة الخدمة الذاتية | 🐞 | احترافية |
| 6 | دمج "الطلب المالي" ضمن منتقي أنواع الطلبات بالبوابة | 🟡 | تجربة موحّدة |
| 7 | موديول "تقييم أداء" حقيقي (Performance Reviews) غير موجود | 🟡 | فجوة مقابل المنافسين |

---

## سجل الإصلاحات

### ✅ 2026-07-26 — توحيد الهوية NEXORA→ZYNORA (ربط بمحرّك Branding)
كل السطوح المرئية التي كانت تكتب الاسم القديم "NEXORA" ثابتاً صارت تقرأ الاسم/الشعار من محرّك الهوية (`IThemeContextService`)، بفولباك "ZYNORA":
- **تخطيط البوابة** `_EmployeePortalLayout.cshtml`: العنوان + الشعار الجانبي (شعار الشركة إن نُشر، وإلا اختصار الاسم) + الترويسة. أُزيل تسريب "إصدار v7" الداخلي.
- **إشعار المخالفة المطبوع** `DisciplinaryRules`: ترويسة "NEXORA HR"+علامة "N" → اسم/اختصار الهوية (مستند يُسلَّم للموظف).
- **eyebrows صفحات الإدارة:** `PositionCategories` · `PositionLevels` · `Positions/Import` · `EmployeeDocuments` · `Employees/{Create,Lifecycle,Delete,EndService,EndServiceList,Rehire}` · `Payroll/TaxSocialSecurity`.
- **متبقٍّ (غير مرئي، مقصود تركه):** مفاتيح `localStorage`/كوكيز `NEXORA.*` + تعليقات كود `NEXORA_FIX*` (تغييرها يخاطر بكسر الجلسات بلا فائدة ظاهرة) + أصول `wwwroot/brand/nexora-*.svg` غير المرجعية.
- **التحقق:** بناء أخضر (0 تحذير) + فحص حي (منفذ 5090): البوابة بلا أي "NEXORA" مرئي، العنوان/الشعار/الترويسة = ZYNORA. ⚠️ فخّ Razor: `Context.RequestAborted` لا يُترجم داخل بلوك `@{ }` بصفحة `@page` (خلاف التخطيطات) — استُخدم `default` كـCancellationToken.
- ⚠️ النسخة المنشورة `C:\ZynoraPortal` تحتاج إعادة نشر.

### ✅ 2026-07-26 — ربط تبويب «التقييم والانضباط» ببيانات حقيقية
**الملفات:** `EmployeePortal/Index.cshtml.cs` (+`DisciplinaryRecord` + `LoadDisciplinaryHistoryAsync` + استدعاء بـ`LoadAsync`) · `EmployeePortal/Index.cshtml` (تبويب performance + مؤشر النبض).
- **الانضباط:** صار يقرأ من `EmployeeViolationCases` (رقم مرجعي، تاريخ، فئة، عنوان، الحالة، الإجراء، العقوبة النهائية، الاقتطاع المالي، ردّ الموظف) بحالة فارغة صادقة ("سجلّك نظيف") عند عدم وجود مخالفات.
- **التقييم:** أُزيلت نقاط القوة الثابتة ("الالتزام، التعاون، سرعة التعلم") واستُبدلت برسالة صادقة (الوحدة لم تُفعّل بعد).
- **مؤشر النبض:** أُزيل الرقم الثابت `78%` واستُبدل بنسبة مشاركة محسوبة (صوّت الموظف في N من M استبيان)، مع حالة "لم تُنشر استبيانات بعد".
- **التحقق:** بناء أخضر + فحص حي على سيرفر تطوير (منفذ 5090) بحالتين (فارغة/مملوءة بصف اختباري مؤقت حُذف بعدها). ⚠️ النسخة المنشورة `C:\ZynoraPortal` تحتاج إعادة نشر لتظهر فيها.
