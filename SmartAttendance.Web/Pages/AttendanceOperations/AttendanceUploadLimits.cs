namespace SmartAttendance.Web.Pages.AttendanceOperations;

/// <summary>
/// حدّ حجم رفع ملفات أجهزة البصمة.
///
/// مقيسٌ لا مُخمَّن: تفريغ 10,000 موظف عبر سبعة أشهر = 2,887,929 بصمة في
/// 131 ميغابايت. الحدّ 512 م.ب يترك هامشاً معقولاً (نحو أربعة أضعاف) ويبقى
/// حدّاً — لا رفعاً للسقف بلا نهاية.
///
/// ⚠️ يُطبَّق بوسم على صفحة عمليات الحضور وحدها. لا ترفعه عالمياً: كل نقطة
/// بالنظام تصير عندها قابلة لاستنزاف القرص والذاكرة برفعة واحدة.
/// </summary>
internal static class AttendanceUploadLimits
{
    /// <summary>512 ميغابايت — ثابت وقت ترجمة ليصلح داخل وسم.</summary>
    public const long MaxUploadBytes = 512L * 1024 * 1024;
}
