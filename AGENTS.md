# AGENTS.md — أدوار وكلاء التطوير والمراجعة

> **اقرأ أولاً: [`docs/AI-DEVELOPMENT-RULES.md`](docs/AI-DEVELOPMENT-RULES.md)** —
> مصدر الحقيقة الملزم. ما هنا تفاصيل تعيين دور الوكيل النشط.

## الدور

يحدّد طلب المهمة الحالي دور الوكيل النشط. **لا يجوز للوكيل التنفيذ إلا إذا عيّنه
المستخدم صراحةً وكيلاً منفّذاً.** والوكيل المعيّن للمراجعة يبقى للقراءة والمراجعة فقط.

- ممنوع أن يعدّل وكيلان منفّذان نفس شجرة العمل في الوقت نفسه.
- إن كان الوكيل للمراجعة فلا ينفّذ. وإن عيّنه المستخدم للتنفيذ فيستخدم فرعاً/شجرة عمل
  معزولة بعد التأكد من عدم وجود منفّذ آخر على الشجرة نفسها.
- ممنوع الدفع لـ`main`، وممنوع النشر، وممنوع الوصول لبيانات الإنتاج.

## قائمة المراجعة الإلزامية لكل PR

### أمان الوصول
- [ ] كل استعلام موظف يفرض نطاق التخويل — لا تحميل بالمعرّف وحده (IDOR).
- [ ] كل استعلام متعدد الشركات يفرض `CompanyId`.
- [ ] المعرّفات من المسار/النموذج/الكويري تُتحقق ملكيتها قبل أي قراءة أو كتابة.
- [ ] لا «جلب الكل ثم ترشيح بالذاكرة».

### الأسرار والخصوصية
- [ ] لا بيانات اعتماد/توكنات/سلاسل اتصال/عناوين إنتاج بالتعديل.
- [ ] لا بيانات موظفين حقيقية بالكود أو الاختبارات أو التوثيق.
- [ ] لا تسجيل لكلمات مرور أو توكنات أو رواتب أو أرقام وطنية أو بيانات بنكية.
- [ ] ملفات الموظفين الخاصة ليست تحت مسار عام.

### الأعمال الحرجة
- [ ] مسّ الرواتب/الحضور/الإجازات/القروض/المالية ⟵ اختبارات انحدار موجودة.
- [ ] لا تغيير غير مطلوب بالصيغ أو الأرصدة أو قواعد الحضور.

### قاعدة البيانات
- [ ] لا جدول/عمود جديد عبر الشفاء الذاتي وقت التشغيل.
- [ ] تغييرات المخطط بهجرة محكومة وموثّقة، لا تعمل مع كل طلب.

### الكتابة والموثوقية
- [ ] الإرسال المكرر/إعادة المحاولة/المعاملات/idempotency مدروسة.

### الجودة والمنتج
- [ ] البناء والاختبارات خضراء (وليست «خضراء كاذبة» بلا تشغيل).
- [ ] RTL وهوية ZYNORA سليمة.
- [ ] لا وظيفة محذوفة بلا سبب موثّق.

## ما يُرفض فوراً

- دفع مباشر لـ`main` أو دمج بلا مراجعة.
- نشر بلا موافقة صريحة لتلك العملية.
- أي سرّ حقيقي بالمستودع.
- تعديل بيانات/قاعدة الإنتاج.
- ادّعاء عزل شركات كامل بلا اختبارات تُثبته.
